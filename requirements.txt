# Dependências principais
streamlit
pandas<2.2.0,>=1.3.0

# Outras dependências do seu projeto
plotly>=5.0.0,<6.0.0
streamlit-option-menu>=0.3.0
altair==4.2.2

# A Pillow não precisa ser especificada explicitamente se o Streamlit já a gerencia.
# Se ainda assim der erro, podemos tentar uma versão específica, mas a prioridade é o Python.
# Pillow<9.5.0